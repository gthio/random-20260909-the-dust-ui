# Program Record: `<PID>-<short-name>`

_Generated by `/refiner-program` from `<requirement path, e.g. .docs/requirements/<ID>-program-<short-name>.md>`._

- **PID:** `<YYYYMMDD-NN>`
- **Status:** active <!-- active | superseded. Freeze state lives in § 5, per contract — never here. -->
- **Last run:** `<YYYY-MM-DD>` <!-- mode of each run is in its Run Log entry (§ 8) -->

> **Scope rule:** This file records *who owes what, in what order* — program
> stories, the split behind them, and where each sub-story landed. It is **not**
> a status board: each repo's own `.docs/board.md` owns its rows, and this file
> never mirrors them. Status here appears only as dated snapshots in the Run Log
> (§ 8), never as a live claim. Per-repo slicing decisions belong to that repo's
> Refiner, not here.

## 1. Requirement

- **Source:** `<committed path — e.g. .docs/requirements/<ID>-program-<short-name>.md>`
  <!-- always a committed path: inline pastes are transcribed to
       .docs/requirements/ before articulation. Amendment runs key on
       this path — it is the requirement's durable anchor. -->
- **In one sentence:** `<the user-observable outcome the requirement promises>`
- **Actors:** `<who acts, who is affected>`
- **Stated constraints:** `<limits the source actually states — not inferred>`

<!-- Articulate, don't invent. Anything the source is silent on goes to § 7. -->

## 2. Program Stories

_Thin, end-to-end, user-observable slices — written before any repo is named
(SLICE-001 at program altitude). Each maps to at most one contract delta._

### Story 1 — `<title>`

- **As a** `<actor>` **I can** `<observable outcome>` **so that** `<value>`.
- **Acceptance at the product boundary:** `<what a user sees work, end to end>`
- **Depends on:** `<story number, or "—">`

### Story 2 — `<title>`

<!-- Repeat per story. If the whole requirement is one thin behavior, emit one
     story — do not invent slices to look thorough. -->

## 3. Split Rationale

_Why this split and not another. Required — an unexplained split cannot be
reviewed, only accepted._

- **Repos considered:** `<roles from the AI.md repo map that this requirement could plausibly touch>`
- **Split chosen:** `<one line per story: which repos own it and why>`
- **Alternatives rejected:** `<the other viable split(s), and the reason each lost — e.g. "split by layer (API story / DB story) rejected: no story delivers user-observable value alone, violates SLICE-001">`
- **Rules applied:** `<SLICE-* codes with a one-line rationale each; api-design / security / resilience codes for the contract delta>`
- **Ownership calls:** `<any judgment the repo map's ownership note did not settle — e.g. where persistence landed and why>`

<!-- If the requirement touched only one repo, say so here: record the
     pass-through, note that no contract delta was needed, and stop. Do not
     manufacture program machinery for single-repo work. -->

## 4. Story Index

_Resolvable, not just descriptive: every row carries enough to check that
repo's live state in one hop._

| Story | Role      | Repo path              | Local ID        | Sub-story                                                |
| :---- | :-------- | :--------------------- | :-------------- | :------------------------------------------------------- |
| 1     | `ui`      | `<../your-ui-repo>`    | `<YYYYMMDD-NN>` | `.docs/backlog/<local-ID>-program-story-<short-name>.md`  |
| 1     | `service` | `<../your-svc-repo>`   | `<YYYYMMDD-NN>` | `.docs/backlog/<local-ID>-program-story-<short-name>.md`  |

<!-- The repo's board is always `<repo path>/.docs/board.md` — derived, not a
     column. -->

<!-- Local IDs are minted per repo, by that repo's own backlog convention. The
     program <PID> travels in sub-story metadata and commit messages, never as a
     sibling's row ID. -->

**Skipped repos:** `<repo: reason (e.g. "service: dirty working tree at run time — not written"), or "none">`

## 5. Interface Index

_Every boundary this program changes, and the contract that pins it. One row per
**crossed** boundary — not an inventory of every boundary in the system._

| Boundary            | Kind        | Producer   | Consumer(s) | Contract                                    | Status |
| :------------------ | :---------- | :--------- | :---------- | :------------------------------------------ | :----- |
| `<ui → service>`    | HTTP API    | `service`  | `ui`        | `.docs/contracts/<short-name>-api-contract.md` | Draft  |
| `<service → db>`    | DB schema   | `db`       | `service`   | `.docs/contracts/<boundary-name>-interface-contract.md` | Draft  |

<!-- HTTP surfaces use the human-contract-api.md shape; every other crossed
     boundary (DB schema, events/topics, shared types or SDK package, file
     format, handshake, cross-repo config keys) uses human-contract-interface.md.
     Omit this section entirely when no boundary is crossed (single-repo degrade). -->

**Freeze gate:** `<N>` interface(s), `<N>` Frozen, `<N>` Draft. Queuing
sub-stories against a `Draft` is normal; no repo's Refiner should *run*
against one.

## 6. Dependency Map

_Which sub-story blocks which, and where the contract freeze sits._

| Blocked                        | Waits on                        | Why                                    |
| :----------------------------- | :------------------------------ | :------------------------------------- |
| `<repo> story <n>`             | contract `<name>` → `Frozen`    | `<builds against the wire shape>`      |
| `<repo> story <n>`             | `<repo> story <n>`              | `<needs the endpoint to exist>`        |

<!-- A preferred merge order is fine (SLICE-002); a value dependency on a
     sibling is not. If a story only delivers value once another ships, it was
     sliced wrong — fix the split, don't record the dependency. -->

## 7. Open Questions

_Gaps in the requirement, surfaced rather than filled. Each names who decides._

- `<question>` — **decides:** `<human / which repo>`

## 8. Run Log

_Append one entry per run. Never rewrite a prior entry._

### Run 1 — `<YYYY-MM-DD>` (`<mode>`)

- **Did:** `<N stories articulated; M repos written; contract delta drafted>`
- **Skipped:** `<repo: reason, or "none">`
- **Status snapshot (as of this run):** `<role: board state, per repo — e.g. "ui: Queued, service: In Progress">`

<!-- The snapshot is a dated observation, read from each sibling's board at run
     time. It is stale the moment that repo's Refiner runs. Never present it as
     current state, and never update it outside a run entry. -->

### Run 2 — `<YYYY-MM-DD>` (amendment)

- **Contract change:** `<which contract(s) amended, and how — section back to Draft, or version bump>`
- **Superseded in place:** `<queued sub-stories rewritten>`
- **Delta queued behind in-flight work:** `<repo: new sub-story, because the old one is In Progress / Done>`
- **Status snapshot (as of this run):** `<role: board state, per repo>`
