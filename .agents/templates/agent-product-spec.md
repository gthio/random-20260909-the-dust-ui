# Product: <Product Name>

_Generated by `/founder-architect` from `<brief path — e.g. .docs/requirements/<ID>-brief-<short-name>.md; the brief stays there as the amendment anchor a later /founder-replan reads>`._

> **Scope rule:** This file holds product intent plus the architectural decisions **derived from** product constraints. Generic engineering rules live in the knowledge repo (entry: `README-AGENT.md`); engineering inventory (services, adapters, commands) lives in `AI.md`; per-feature design lives in `.docs/active/.../design.md`. If a technical decision can't be traced to a product constraint here, it belongs in one of those other docs, not this one.

## 1. Vision

- **For whom:** <persona>
- **Problem:** <pain point>
- **Success looks like:** <MVP done condition>

## 2. Architectural Decisions Driven by Constraints

_One row per hard constraint from the brief, paired with the architectural decision it forces. Downstream Architects inherit these — do not re-litigate per feature spec. **If a row can't name a specific constraint from Section 1, it doesn't belong here** — push it to the knowledge repo's conventions or a per-feature design doc._

| Constraint                                   | Architectural Decision                                |
| :------------------------------------------- | :---------------------------------------------------- |
| <e.g., human clears Cloudflare>              | <e.g., headed Playwright; session persistence>        |

## 3. Walking Skeleton

**Feature:** `<ID>-delivery-feature-<short-name>.md` — <one sentence; the thinnest E2E slice>.

Build before anything else. It validates the toolchain end-to-end.

## 4. Backlog Map

_Dependency order. Each row references a file in `.docs/backlog/`._

| Order | Feature                                      | Depends on | Notes              |
| :---- | :------------------------------------------- | :--------- | :----------------- |
| 1     | `20260601-01-delivery-feature-<short-name>.md`        | —          | Walking skeleton   |
| 2     | `20260601-02-delivery-feature-<short-name>.md`        | 1          |                    |

## 5. Out of Scope

- <list>

## 6. Open Questions (Human to Decide)

- <e.g., legal/ToS posture for target sites>
