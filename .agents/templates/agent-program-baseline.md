# Program Baseline: `<program name>`

_Generated by `/program-baseline`. Derived from the `Frozen` contract(s) in `.docs/contracts/`, the intake decisions, and each sibling's spec views — and, after a program-refiner run, that program's record. Regenerated in place after every contract freeze; only § 11 rows marked `(human)` and the run log (§ 12) survive a run untouched._

| | |
| :-- | :-- |
| **Program home** | `<this repo's path>` |
| **Contracts** | `.docs/contracts/<name>.md` v`<x.y.z>` `Frozen` <!-- one per line; a Draft contract stops the run --> |
| **Repo map** | `<role → ../path>` <!-- copied from AI.md § Program Repos --> |
| **Decisions** | `.docs/requirements/<ID>-intake-<short-name>.md` § 7 |
| **Last run** | `<YYYY-MM-DD>` — trigger: `<initial | contract <name> v<x.y.z> | program <PID>>` |

> **Scope rule.** This file says what crosses the wire and **who proves each part of it** — the seam no sibling can see alone. Per-repo detail lives in each sibling's spec views and is **linked, never restated**. Nothing here is a decision: a gap becomes a § 11 row with an owner. Siblings build against the `Frozen` contract, not against this file — it is an orientation, not a gate.

## 0. Delta since last run

<!-- Only on a run triggered by a freeze. Omit the section on the initial run. -->

- **Trigger:** `<contract <name> v<old> → v<new> | program <PID>-<short-name>>`
- **Contract sections added or changed:** `<§ list>`
- **Rows added:** § 4 `<n>` · § 5 `<n>` · § 6 `<n>` · § 7 `<n>` (`<j>` joint) · § 8 `<n>`
- **Sub-stories owing the new joint rows:** `<role: <local-ID> → § 7 row <n>>` <!-- from the program record's § 4 Story Index -->

## 1. The system in one paragraph

`<What a user does, what each sibling does for it, what the upstreams are, what is deliberately not stored or retried. One paragraph. Cite the intent files.>`

## 2. Repos and boundaries

| Role | Repo | Ships | Spec views | Owns on the wire |
| :--- | :--- | :---- | :--------- | :--------------- |
| `<role>` | `<../path>` | `<artifact, stack, run command — from that repo's AI.md>` | `<../path>/.docs/spec/view-*.md` | `<produces … / consumes …>` |

| Boundary | Governing document | Status |
| :------- | :----------------- | :----- |
| `<role> ↔ <role>` | `.docs/contracts/<name>.md` v`<x.y.z>` | `Frozen` |
| `<role> ↔ <external>` | `<sibling>/.docs/spec/external-contracts.md § …` | as specified — behind that sibling only |

## 3. Decisions that shape the build

| Q | Decision | Effect on the wire, or on who proves what |
| :- | :------- | :---------------------------------------- |
| `Q-NN` | `<verbatim from the intake record § 7>` | `<one line>` |

## 4. Wire vocabulary

<!-- One row per (status, code, route) the contract defines. The consumer column is the contract's own client-action text — nothing the contract does not say. -->

| Producer emits | When | Consumer does | Contract |
| :------------- | :--- | :------------ | :------- |
| `<status> <code>` on `<route>` | `<condition>` | `<client action>` | `§<n>` |

## 5. Header handshake

| Header | Set by | Read by | Rule | Contract |
| :----- | :----- | :------ | :--- | :------- |
| `<Header-Name>` | `<role>` | `<role>` | `<presence, exposure, direction>` | `§<n>` |

## 6. Shared fixtures

<!-- One row per fixture in the contract's reference-fixtures section. "Unused" is a § 11 row. -->

| Fixture | `<producer role>` uses it for | `<consumer role>` uses it for |
| :------ | :---------------------------- | :---------------------------- |
| `<name>` | `<golden response / validator input / …>` | `<stub body / preview render / …>` |

## 7. Conformance rows by prover

<!-- The contract's conformance checklist, verbatim, numbered in order. Prover is one of: a role from the repo map, `both` (each side independently), `joint` (needs both processes or a browser context — neither repo can prove it alone), or `human gate`. "How" names the suite from that repo's AI.md / spec views. The last column is filled only on a program-triggered run. -->

| # | Row (verbatim) | Prover | How | Sub-story |
| :- | :------------- | :----- | :-- | :-------- |
| 1 | `<row text>` | `<role | both | joint | human gate>` | `<suite or gate>` | `<local-ID or —>` |

## 8. Paired constants

<!-- One row per value that lives on both sides — caps, budgets, names, copy. "Enforced by: nothing" is the default and the reason the row exists. -->

| Pair | `<producer role>` side | `<consumer role>` side | Relationship | Enforced by |
| :--- | :--------------------- | :--------------------- | :----------- | :---------- |
| `<value>` | `<key or constant, from its spec views>` | `<key or constant>` | `§<n>: <rule>` | `nothing | <test> | joint smoke` |

## 9. Local topology

<!-- From each side's configuration surface in its spec views. No process launcher is assumed. -->

| | `<producer role>` | `<consumer role>` |
| :-- | :---------------- | :---------------- |
| Listens on | `<address from config>` | `<address from config>` |
| Points at the other | `<key = value>` | `<key = value>` |
| Must also set locally | `<keys>` | `<keys>` |

## 10. Joint smoke

<!-- Every `joint` row of § 7 and every § 8 row enforced by nothing, as a manual checklist run with both processes up. -->

1. `<observable step>` — proves § 7 row `<n>` / § 8 `<pair>`.

## 11. Open items

<!-- Gaps only: the contract is silent where a side needs a value; a constant is unpinned; a section the views leave unverified. Rows marked `(human)` are preserved across runs. -->

| Item | Owner / where it is decided | State |
| :--- | :-------------------------- | :---- |
| `<gap>` | `<Q-NN | <role> architect | human>` | `<open | decided by default | proposed>` |

## 12. Run log

_Append one entry per run. Never rewrite a prior entry._

### Run 1 — `<YYYY-MM-DD>` (`<trigger>`)

- **Read:** `<contracts + versions; spec views per role; intake record; program record if any; human baseline if any>`
- **Wrote:** `<sections regenerated; rows added / removed>`
- **Flagged:** `<roles with no spec views; Draft contracts refused; unused fixtures>`
